# Core dependencies for TCM-SD-42 public code package
# Python >= 3.10

# Deep learning
torch>=2.5.0
transformers>=4.40.0
peft>=0.10.0              # LoRA fine-tuning
bitsandbytes>=0.43.0       # 4-bit NF4 quantization (CUDA only)

# Evaluation
scikit-learn>=1.4.0       # precision_recall_fscore_support, balanced_accuracy

# Utilities
numpy>=1.26.0
pandas>=2.1.0
pyyaml>=6.0
tqdm>=4.66.0